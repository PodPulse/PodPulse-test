---
# PodPulse Fix Recommendation — OOMKilled

**Incident:** f742a1b4-6fe9-46fa-a85a-96a779fdb066
**Pod:** default/oom-test
**Node:** k3d-bayou-local-server-0
**Detected at:** 2026-04-06T08:47:25.0000000+00:00

## Diagnosis
Pod 'oom-test' in namespace 'default' on node 'k3d-bayou-local-server-0' has been restarted 1 time due to an OOMKilled event. The container 'oom' is configured with a memory limit of 50Mi and was terminated by the kernel OOM killer. Log tail confirms a stress process was dispatching 1 vm (virtual memory) hog at the time of the kill.

## Root Cause
The container 'oom' is running a stress workload that allocates virtual memory (1 vm hog) exceeding the configured memory limit of 50Mi. The Linux kernel OOM killer terminated the container process when memory consumption breached the cgroup limit enforced by Kubernetes. The memory limit of 50Mi is insufficient for the workload being executed inside the container.

## Recommended Change

| Field | Value |
|---|---|
| Resource type | `Pod` |
| Resource name | `oom-test` |
| Namespace | `default` |
| Container | `oom` |
| Current memory limit | `50Mi` |
| Recommended memory limit | `100Mi` |

```yaml
# Apply this change to your Pod manifest:
spec:
  template:
    spec:
      containers:
        - name: oom
          resources:
            limits:
              memory: "100Mi"
```

## Confidence Score
0,93

---
*Generated by PodPulse — review before merging*