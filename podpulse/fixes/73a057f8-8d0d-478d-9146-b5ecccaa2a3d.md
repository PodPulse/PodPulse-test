---
# PodPulse Fix Recommendation — CrashLoopBackOff

**Incident:** 73a057f8-8d0d-478d-9146-b5ecccaa2a3d
**Pod:** vault-nonprod/bayou-vault-agent-injector-79c94f9cdf-s4sv5
**Node:** k3d-bayou-local-server-0
**Detected at:** 2026-04-09T13:27:51.0000000+00:00

## Diagnosis
The vault-agent-injector pod (bayou-vault-agent-injector-79c94f9cdf-s4sv5) in namespace vault-nonprod has restarted 144 times. The final log entries show repeated TLS handshake errors from the Kubernetes API server (10.42.0.1) to the injector webhook endpoint on port 8080, including 'connection reset by peer' and 'EOF' errors. The container has no memory limit set and no OOM kill evidence, indicating the restarts are likely caused by liveness/readiness probe failures or a crash triggered by TLS misconfiguration rather than resource exhaustion.

## Root Cause
The root cause is a TLS certificate mismatch or staleness between the MutatingWebhookConfiguration CA bundle and the certificate currently served by the vault-agent-injector. The certwatcher logs show periodic certificate bundle updates (approximately every 24 hours), and the TLS handshake errors occur immediately after a cert rotation cycle. The Kubernetes API server is rejecting or failing to complete TLS handshakes because the webhook CA bundle registered in the MutatingWebhookConfiguration no longer matches the self-signed certificate being served by the injector (or vice versa). This causes the API server to drop connections, which in turn causes liveness/readiness probe failures or unhandled panics, leading to the high restart count. The auto-TLS mode is regenerating the CA on each restart, creating a cycle where the webhook config is perpetually out of sync.

## Recommended Change

| Field | Value |
|---|---|
| Resource type | `Pod` |
| Resource name | `bayou-vault-agent-injector-79c94f9cdf-s4sv5` |
| Namespace | `vault-nonprod` |
| Container | `` |

## Confidence Score
0.82

---
*Generated by PodPulse — review before merging*