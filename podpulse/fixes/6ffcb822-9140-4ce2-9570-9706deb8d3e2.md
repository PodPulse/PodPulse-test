---
# PodPulse Fix Recommendation — OOMKilled

**Incident:** 6ffcb822-9140-4ce2-9570-9706deb8d3e2
**Pod:** default/oom-test
**Node:** k3d-bayou-local-server-0
**Detected at:** 2026-04-05T18:15:47.0000000+00:00

## Diagnosis
Pod 'oom-test' in namespace 'default' on node 'k3d-bayou-local-server-0' has been restarted 1 time due to an OOMKilled event. Container 'oom' is operating under a 50Mi memory limit and was terminated by the kernel OOM killer.

## Root Cause
The container 'oom' exceeded its configured memory limit of 50Mi, triggering an OOMKill by the Linux kernel. The memory limit is likely too low for the workload's actual memory consumption pattern. Log retrieval failure (containerd ID 17a1c72053ad1021cf12b7daff4f580d7aa3de699db0447af43cd9319a69d830) suggests the container was forcibly terminated before logs could be flushed, which is consistent with an abrupt OOM kill. The absence of owner_kind/owner_name indicates this is a standalone pod with no controller managing restarts beyond the kubelet's restart policy.

## Recommended Change

| Field | Value |
|---|---|
| Resource type | `Pod` |
| Resource name | `oom-test` |
| Namespace | `default` |
| Container | `oom` |
| Current memory limit | `50Mi` |
| Recommended memory limit | `100Mi` |

```yaml
# Apply this change to your Pod manifest:
spec:
  template:
    spec:
      containers:
        - name: oom
          resources:
            limits:
              memory: "100Mi"
```

## Confidence Score
0,91

---
*Generated by PodPulse — review before merging*