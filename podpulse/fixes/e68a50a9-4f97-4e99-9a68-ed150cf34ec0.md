---
# PodPulse Fix Recommendation — OOMKilled

**Incident:** e68a50a9-4f97-4e99-9a68-ed150cf34ec0
**Pod:** default/oom-test
**Node:** k3d-bayou-local-server-0
**Detected at:** 2026-04-06T19:34:48.0000000+00:00

## Diagnosis
Pod 'oom-test' in namespace 'default' on node 'k3d-bayou-local-server-0' has been restarted 1 time due to an OOMKilled event. Container 'oom' has a memory limit of 50Mi and was terminated by the kernel OOM killer when memory usage exceeded the configured limit.

## Root Cause
The container 'oom' exceeded its memory limit of 50Mi, triggering an OOMKill by the Linux kernel. The application running inside the container likely has a memory leak, is processing a larger-than-expected workload, or the configured memory limit of 50Mi is insufficient for the application's baseline or peak memory requirements. Log retrieval failed (containerd runtime error), so exact memory usage at kill time is unavailable, but the OOMKill restart pattern is consistent with memory over-consumption relative to the 50Mi cap.

## Recommended Change

| Field | Value |
|---|---|
| Resource type | `Pod` |
| Resource name | `oom-test` |
| Namespace | `default` |
| Container | `oom` |
| Current memory limit | `50Mi` |
| Recommended memory limit | `100Mi` |

## Confidence Score
0.88

---
*Generated by PodPulse — review before merging*