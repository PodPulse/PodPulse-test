---
# PodPulse Fix Recommendation — OOMKilled

**Incident:** df31c3c2-2ac3-4d8d-b163-ea8d59ffdc35
**Pod:** default/oom-test
**Node:** k3d-bayou-local-server-0
**Detected at:** 2026-04-05T18:43:31.0000000+00:00

## Diagnosis
Pod 'oom-test' in namespace 'default' on node 'k3d-bayou-local-server-0' has been restarted 1 time due to an OOMKilled event. Container 'oom' was terminated by the Linux kernel OOM killer after exceeding its configured memory limit of 50Mi.

## Root Cause
The container 'oom' consumed memory beyond its configured limit of 50Mi, triggering an OOMKill by the kernel. The memory limit of 50Mi is likely insufficient for the workload running inside the container. Log retrieval failed (containerd log reference: 59801ab962120f4b4b0680e5a74074368cd84e5a9f49548d3af005de52db2a01), which may indicate the container was killed abruptly before logs could be flushed, or the containerd shim lost track of the container state post-kill. No owner (Deployment/ReplicaSet/DaemonSet) is associated with this pod, suggesting it is a standalone pod with no automatic rescheduling guarantees beyond the pod restart policy.

## Recommended Change

| Field | Value |
|---|---|
| Resource type | `Pod` |
| Resource name | `oom-test` |
| Namespace | `default` |
| Container | `oom` |
| Current memory limit | `50Mi` |
| Recommended memory limit | `100Mi` |

```yaml
# Apply this change to your Pod manifest:
spec:
  template:
    spec:
      containers:
        - name: oom
          resources:
            limits:
              memory: "100Mi"
```

## Confidence Score
0,82

---
*Generated by PodPulse — review before merging*