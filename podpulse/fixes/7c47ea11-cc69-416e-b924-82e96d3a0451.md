---
# PodPulse Fix Recommendation — OOMKilled

**Incident:** 7c47ea11-cc69-416e-b924-82e96d3a0451
**Pod:** default/oom-test
**Node:** k3d-bayou-local-server-0
**Detected at:** 2026-04-05T18:13:04.0000000+00:00

## Diagnosis
Pod 'oom-test' in namespace 'default' on node 'k3d-bayou-local-server-0' has been restarted 1 time due to an OOMKilled event. Container 'oom' has a memory limit of 50Mi and was terminated by the kernel OOM killer when memory usage exceeded the configured limit.

## Root Cause
The container 'oom' exceeded its configured memory limit of 50Mi, triggering an OOMKill by the Linux kernel. The memory limit of 50Mi is likely insufficient for the workload running inside the container. The absence of 'memory_used_at_kill' data and unrecoverable container logs (containerd runtime log retrieval failure) suggest the container was forcefully terminated before logs could be flushed, which is consistent with a sudden OOM event. The low restart count of 1 indicates this is an early-stage or newly deployed workload that has not yet been tuned for memory consumption.

## Recommended Change

| Field | Value |
|---|---|
| Resource type | `Pod` |
| Resource name | `oom-test` |
| Namespace | `default` |
| Container | `oom` |
| Current memory limit | `50Mi` |
| Recommended memory limit | `100Mi` |

```yaml
# Apply this change to your Pod manifest:
spec:
  template:
    spec:
      containers:
        - name: oom
          resources:
            limits:
              memory: "100Mi"
```

## Confidence Score
0,87

---
*Generated by PodPulse — review before merging*