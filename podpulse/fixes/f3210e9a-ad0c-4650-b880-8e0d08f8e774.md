---
# PodPulse Fix Recommendation — OOMKilled

**Incident:** f3210e9a-ad0c-4650-b880-8e0d08f8e774
**Pod:** default/oom-test
**Node:** k3d-bayou-local-server-0
**Detected at:** 2026-04-03T20:41:13.0000000+00:00

## Diagnosis
Pod 'oom-test' in namespace 'default' on node 'k3d-bayou-local-server-0' has been restarted 1 time due to an OOMKilled event. Container 'oom' has a memory limit of 50Mi and was terminated by the kernel OOM killer. Log retrieval failed, indicating the container was killed before logs could be flushed.

## Root Cause
The container 'oom' exceeded its configured memory limit of 50Mi, triggering an OOMKill by the Linux kernel. The low memory ceiling (50Mi) is likely insufficient for the workload running inside the container. The absence of 'memory_used_at_kill' data and the containerd log retrieval failure suggest the container was abruptly terminated, consistent with an OOM event. No owner (Deployment/ReplicaSet) is recorded, meaning this is a standalone pod with no automatic rescheduling beyond the pod restart policy.

## Recommended Change

| Field | Value |
|---|---|
| Resource type | `Pod` |
| Resource name | `oom-test` |
| Namespace | `default` |
| Container | `oom` |
| Current memory limit | `50Mi` |
| Recommended memory limit | `100Mi` |

```yaml
# Apply this change to your Pod manifest:
spec:
  template:
    spec:
      containers:
        - name: oom
          resources:
            limits:
              memory: "100Mi"
```

## Confidence Score
0,87

---
*Generated by PodPulse — review before merging*