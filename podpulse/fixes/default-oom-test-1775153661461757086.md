---
# PodPulse Fix Recommendation — OOMKilled

**Incident:** default-oom-test-1775153661461757086
**Pod:** default/oom-test
**Node:** k3d-bayou-local-server-0
**Detected at:** 2026-04-02T18:14:21.0000000+00:00

## Diagnosis
Pod 'oom-test' in namespace 'default' on node 'k3d-bayou-local-server-0' was OOMKilled. Container 'oom' has restarted 1 time. The container is configured with a memory limit of 50Mi. Log retrieval failed, suggesting the container was terminated before logs could be captured by the runtime.

## Root Cause
The container 'oom' exceeded its configured memory limit of 50Mi, triggering the Linux OOM killer to terminate the process. The 50Mi limit is likely insufficient for the workload running inside the container. The absence of memory_used_at_kill data and failed log retrieval (containerd container ID 40210b14d0919a29e9d57ee7b6b8f446cf8384576a7b3ffb01a376383d153186) indicates the container was killed abruptly, possibly during startup or a memory spike, before metrics and logs could be flushed.

## Recommended Change

| Field | Value |
|---|---|
| Resource type | `Pod` |
| Resource name | `oom-test` |
| Namespace | `default` |
| Container | `oom` |
| Current memory limit | `50Mi` |
| Recommended memory limit | `100Mi` |

```yaml
# Apply this change to your Pod manifest:
spec:
  template:
    spec:
      containers:
        - name: oom
          resources:
            limits:
              memory: "100Mi"
```

## Confidence Score
0,82

---
*Generated by PodPulse — review before merging*