---
# PodPulse Fix Recommendation — OOMKilled

**Incident:** c2b2e5a7-53c2-4fa1-9d12-f78609423dd4
**Pod:** default/oom-test
**Node:** k3d-bayou-local-server-0
**Detected at:** 2026-04-05T19:29:16.0000000+00:00

## Diagnosis
Pod 'oom-test' in namespace 'default' on node 'k3d-bayou-local-server-0' has been restarted 1 time. The container 'oom' was killed due to an OOMKilled event, indicating it exceeded its configured memory limit of 50Mi.

## Root Cause
The container 'oom' consumed more memory than its configured limit of 50Mi, triggering the Linux OOM killer. This is a classic Out-Of-Memory kill scenario where the application's memory footprint exceeds the Kubernetes resource limit defined in the pod spec. The memory_used_at_kill field is empty, likely due to a log retrieval failure from containerd, but the restart pattern and incident type confirm OOMKill as the cause. The application may have a memory leak, be processing unexpectedly large payloads, or the memory limit may simply be set too low for the workload.

## Recommended Change

| Field | Value |
|---|---|
| Resource type | `Pod` |
| Resource name | `oom-test` |
| Namespace | `default` |
| Container | `oom` |
| Current memory limit | `50Mi` |
| Recommended memory limit | `100Mi` |

```yaml
# Apply this change to your Pod manifest:
spec:
  template:
    spec:
      containers:
        - name: oom
          resources:
            limits:
              memory: "100Mi"
```

## Confidence Score
0,88

---
*Generated by PodPulse — review before merging*