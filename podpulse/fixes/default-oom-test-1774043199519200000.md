---
# PodPulse Fix Recommendation — OOMKilled

**Incident:** default-oom-test-1774043199519200000
**Pod:** default/oom-test
**Node:** k3d-bayou-local-server-0
**Detected at:** 2026-03-20T21:46:39.0000000+00:00

## Diagnosis
Pod 'oom-test' in namespace 'default' on node 'k3d-bayou-local-server-0' has been OOMKilled 2 times. The container 'oom' is running a memory stress workload (stress tool with 1 vm worker) and is configured with a 50Mi memory limit. Log tail confirms the stress process was actively allocating memory at the time of kill.

## Root Cause
The container 'oom' is executing a 'stress' command with a VM worker (1 vm hog) that allocates memory beyond the configured 50Mi limit. The stress tool is intentionally or unintentionally consuming more memory than the container's limit allows, triggering the Linux OOM killer. Since memory_used_at_kill is not captured, the exact overage is unknown, but the stress vm worker by default attempts to allocate and stress all available memory, which will always exceed a 50Mi cap.

## Recommended Change

| Field | Value |
|---|---|
| Resource type | `Pod` |
| Resource name | `oom-test` |
| Namespace | `default` |
| Container | `oom` |
| Current memory limit | `50Mi` |
| Recommended memory limit | `100Mi` |

```yaml
# Apply this change to your Pod manifest:
spec:
  template:
    spec:
      containers:
        - name: oom
          resources:
            limits:
              memory: "100Mi"
```

## Confidence Score
0,88

---
*Generated by PodPulse — review before merging*