---
# PodPulse Fix Recommendation — OOMKilled

**Incident:** fdda11bc-f31e-4704-bd75-3c61f8df6c96
**Pod:** default/oom-test
**Node:** k3d-bayou-local-server-0
**Detected at:** 2026-04-05T18:32:02.0000000+00:00

## Diagnosis
Pod 'oom-test' in namespace 'default' on node 'k3d-bayou-local-server-0' has been restarted 1 time due to an OOMKilled event. Container 'oom' is operating under a 50Mi memory limit and was terminated by the kernel OOM killer.

## Root Cause
The container 'oom' exceeded its configured memory limit of 50Mi, triggering an OOMKill by the Linux kernel. The memory limit of 50Mi is likely insufficient for the workload running inside the container. Log retrieval failure (containerd runtime error for container ID 71c93c7583de247831f27378fe78d85c60224f63dbb40469c6c6df53fe14cd87) suggests the container was forcibly terminated before logs could be flushed, which is consistent with an abrupt OOM kill. The absence of owner_kind/owner_name indicates this is a standalone pod with no controller managing restarts beyond the pod's own restart policy.

## Recommended Change

| Field | Value |
|---|---|
| Resource type | `Pod` |
| Resource name | `oom-test` |
| Namespace | `default` |
| Container | `oom` |
| Current memory limit | `50Mi` |
| Recommended memory limit | `100Mi` |

```yaml
# Apply this change to your Pod manifest:
spec:
  template:
    spec:
      containers:
        - name: oom
          resources:
            limits:
              memory: "100Mi"
```

## Confidence Score
0,87

---
*Generated by PodPulse — review before merging*